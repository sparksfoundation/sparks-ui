import React from "react";
type ModalHeaderProps = {
    closeModal?: () => void;
    heading: string;
};
export declare const ModalHeader: ({ closeModal, heading }: ModalHeaderProps) => React.JSX.Element;
export {};
